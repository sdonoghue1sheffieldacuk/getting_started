[project]
name = "getting-started2"
version = "0.1.0"
description = "this is my descriptionns"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cowsay>=6.1",
    "torch>=2.8.0",
    "transformers>=4.56.2",
]
